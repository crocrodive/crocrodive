@props(['type', 'name', 'placeholder'])
<div>
    <input class="rounded-cards text-h4 border-10 px-4 py-2 " type="{{ $type }}" name="{{ $name }}" id="{{ $name }}" placeholder="{{ $placeholder }}">
</div>
